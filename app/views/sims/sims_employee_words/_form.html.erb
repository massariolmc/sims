<%= form_for(sims_employee_word) do |f| %>
  <% if sims_employee_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sims_employee_word.errors.count, "error") %> prohibited this sims_employee_word from being saved:</h2>

      <ul>
      <% sims_employee_word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
