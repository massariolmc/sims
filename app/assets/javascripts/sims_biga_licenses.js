$(document).ready(function(){
	//$(document).on('turbolinks:load', function() {
		$( "#sims_biga_license_cnh_pes_id" ).autocomplete({
  			source: $('#sims_biga_license_cnh_pes_id').data('autocomplete-source')
		});

	//});
});